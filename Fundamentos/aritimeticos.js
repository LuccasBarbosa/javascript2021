const [a,b,c,d] = [1,2,3,4]

let soma = a + b + c + d
let subtracao = a - b
let multiplicacao = a * b 
let divisao = a / b 
let modulo = a % b 

console.log(soma, subtracao, multiplicacao, divisao, modulo);